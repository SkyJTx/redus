name: redus_flutter
description: Vue-like Component system for Flutter with reactive state, lifecycle hooks, and dependency injection.
version: 0.7.1
repository: https://github.com/SkyJTx/redus
homepage: https://github.com/SkyJTx/redus/tree/main/redus_flutter
issue_tracker: https://github.com/SkyJTx/redus/issues
topics:
  - flutter
  - state-management
  - component
  - lifecycle
  - dependency-injection

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.9.0
  redus: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
