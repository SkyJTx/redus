/// Extensions barrel file - exports all extension methods.
///
/// Provides `.watch(context)` extension methods on [Ref] and [Computed]
/// for fine-grained reactivity in Flutter.
library;

export 'reactive_watch.dart';
